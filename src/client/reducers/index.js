import ACTIONS from '../actions';
// Import reducers functions from reducer files

/*
** Initial state
*/

const initialState = {
};

/*
** Reducer
*/
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
